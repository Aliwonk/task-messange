const HOST_BACKEND = "http://essey-dev.site";
export const API = {
    AUTH: {
        REGISTRATION: `${HOST_BACKEND}/auth/registration`,
        LOGIN: `${HOST_BACKEND}/auth/login`
    },
    USER: {
        PROFILE: `${HOST_BACKEND}/user/profile`,
    }
};